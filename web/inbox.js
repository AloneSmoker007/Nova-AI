document.addEventListener("DOMContentLoaded",()=>{document.querySelectorAll(".conversation").forEach(item=>item.addEventListener("click",()=>{document.querySelectorAll(".conversation").forEach(x=>x.classList.remove("active"));item.classList.add("active")}));document.querySelectorAll(".filter,.chip").forEach(item=>item.addEventListener("click",()=>{const group=item.parentElement;group.querySelectorAll("button").forEach(x=>x.classList.remove("active"));item.classList.add("active")}));document.querySelector(".ask-nova")?.addEventListener("click",()=>alert("Nova Assistant: the command/action layer will connect to backend permissions in the next stage."));});