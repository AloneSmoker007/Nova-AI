document.addEventListener("DOMContentLoaded",async()=>{const host=document.querySelector(".feature-grid article");if(!host)return;const esc=v=>String(v??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;","\"":"&quot;","'":"&#39;"}[c]));try{const r=await NovaAPI.automations();const rows=r.data||[];host.innerHTML='<div class="panel-head"><h2>Workflows</h2><span class="tag green">'+rows.length+" loaded</span></div>"+(rows.length?rows.map(w=>'<div class="workflow"><div class="flow-icon">◌</div><div><strong>'+esc(w.name)+'</strong><small>'+esc(w.trigger_type)+' · v'+esc(w.version)+'</small><span class="tag '+(w.status==="active"?"green":"amber")+'">'+esc(w.status)+'</span></div><button class="row-action" data-id="'+w.id+'" data-status="'+(w.status==="active"?"paused":"active")+'">'+(w.status==="active"?"Pause":"Activate")+"</button></div>").join(""):'<p class="muted">No workflows configured yet.</p>');host.querySelectorAll("[data-id]").forEach(b=>b.addEventListener("click",async()=>{b.disabled=true;try{await NovaAPI.updateAutomationStatus(b.dataset.id,b.dataset.status);location.reload();}catch(e){alert(e.message);b.disabled=false;}}));}catch(e){host.innerHTML='<p class="muted">'+esc(e.message)+"</p>";}});