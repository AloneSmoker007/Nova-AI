document.addEventListener("DOMContentLoaded",()=>{const a=document.querySelector("#appointments"),p=document.querySelector("#payment-list");const esc=v=>String(v??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;","\"":"&quot;","'":"&#39;"}[c]));const money=(v,c="PKR")=>{const n=Number(v);return Number.isFinite(n)?new Intl.NumberFormat("en-PK",{style:"currency",currency:c,maximumFractionDigits:0}).format(n/100):"—"};
async function appointments(){a.innerHTML="<p class='muted'>Loading…</p>";try{const r=await NovaAPI.appointments({from:new Date().toISOString()});const rows=r.data||[];a.innerHTML=rows.length?rows.map(x=>'<div class="notice"><b>'+esc(x.customer_name)+'</b><small>'+new Date(x.starts_at).toLocaleString()+" · "+esc(x.status)+" · "+esc(x.customer_phone)+'</small><span><button class="secondary" data-a="'+x.id+'" data-s="'+(x.status==="confirmed"?"completed":"confirmed")+'">'+(x.status==="confirmed"?"Complete":"Confirm")+"</button></span></div>").join(""):'<p class="muted">No upcoming appointments.</p>';a.querySelectorAll("[data-a]").forEach(b=>b.addEventListener("click",async()=>{b.disabled=true;try{await NovaAPI.updateAppointmentStatus(b.dataset.a,b.dataset.s);await appointments();}catch(e){alert(e.message);b.disabled=false;}}));}catch(e){a.innerHTML='<p class="muted">'+esc(e.message)+"</p>";}}
async function payments(){p.innerHTML="<p class='muted'>Loading…</p>";try{const r=await NovaAPI.payments({limit:100});const rows=r.data||[];p.innerHTML=rows.length?rows.map(x=>'<div class="notice"><b>'+esc(x.customer_name||x.provider_payment_id)+'</b><small>'+esc(x.provider)+" · "+esc(x.status)+" · "+esc(x.currency)+"</small><span>"+money(x.amount_minor,x.currency)+"</span></div>").join(""):'<p class="muted">No payments found.</p>';}catch(e){p.innerHTML='<p class="muted">'+esc(e.message)+"</p>";}}
document.querySelector("#refresh-appointments")?.addEventListener("click",appointments);document.querySelector("#refresh-payments")?.addEventListener("click",payments);appointments();payments();});